# NEXT — 次のセッションで再開するための引き継ぎ

最終更新: 2026-09-03

このファイルは別のPCや次のセッションで作業を再開する時、まず読むためのもの。完了したら該当項目を削除する。

## 進行中: 動作絵(動詞)・情景絵の追加 — 画像取り込み済み、音声生成待ち

- 動作20枚・情景14枚を生成・最適化し、`cards.json`(176語)と `categories.json`(動作・情景)に取り込み済み
- アプリの情景対応(ヒントなし・模範文・対の絵を選択肢に)は実データで検証済み
- 元画像(1024px)のバックアップはリポジトリ外(作業PCの一時領域)。再最適化が必要なら Codex で再生成する

### 残作業

- [ ] **音声生成**: VOICEVOX を起動して `node scripts/generate-audio.mjs`(34件の差分。情景は `tts_text` の漢字文で合成)。生成されるまで該当語は Web Speech でフォールバック再生される
- [ ] 音声を試聴し、情景文のアクセント・区切りが不自然なら `scripts/generate-audio.mjs` の `TTS_OVERRIDES` で読みを調整して該当 wav を削除→再実行
- [ ] `sw.js` の `CACHE_NAME` と `app.js` の `APP_VERSION` を上げてコミット・プッシュ(画像取り込み時点で v26 に上げ済み。音声追加時にもう1つ上げる)
- [ ] iPad で確認: 呼称(動作/情景)、選択で情景のみ→対の絵が選択肢に出るか、線つなぎで情景の長文が収まるか
- [ ] ST に試してもらい、分かりにくい絵は `codex_image_generation_guide.md` §4.3 の要領で個別再生成

### 保留(次の候補)

- 名詞カードの増量(スタッフから追加語リストを集める)。`data/pending/` に同じ形式で置けば `merge-pending.mjs` で取り込める
- 情景絵の出題の流れ(模範文を隠しておいて患者が説明した後に表示、など)は ST の使用感を聞いてから調整
- 短文理解を独立した課題にするか(現状は「選択モード × 情景カテゴリ」で代用)
