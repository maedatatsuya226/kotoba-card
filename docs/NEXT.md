# NEXT — 次のセッションで再開するための引き継ぎ

最終更新: 2026-09-04

このファイルは別のPCや次のセッションで作業を再開する時、まず読むためのもの。完了したら該当項目を削除する。

## 完了: 課題構成の再整理(v28)

`TASK_STRUCTURE_DISCUSSION.md` の議論を実装した。判断の要約は `DECISIONS.md` §17。

- ホーム: 「話す / 選ぶ / つなぐ」の3入口 + 各入口の下に7つの課題プリセット
- 設定画面: 総問題数スライダーと難易度を常時表示、他は「詳細設定」に折りたたみ(現在値を1行要約、開閉状態を端末に記憶)
- 制限時間: スライダー(0〜30秒)
- 情景説明: 「自立 / 手がかりあり / 困難」の3段階評価、終了画面に帯表示
- プリセットの初期値は `app.js` の `TASK_PRESETS`

### 残作業

- [ ] iPad 実機で確認: ホームのプリセット→スタートの流れ、詳細設定の開閉、制限時間スライダーの操作感、情景の3段階評価
- [ ] ST に試してもらい、プリセットの初期値(教材・問題数・選択肢数)を要望に合わせて `TASK_PRESETS` で調整
- [ ] 「単語を聞いて絵を選ぶ」の教材を名詞+動作にしているが、名詞のみが良ければ `categories: 'core'` に変更

## 完了: 動作絵(動詞)・情景絵の追加(v26〜v27)

- 動作20枚・情景14枚を取り込み済み(176語)、音声も全件同梱
- 元画像(1024px)のバックアップはリポジトリ外(作業PCの一時領域)。再最適化が必要なら Codex で再生成する

### 残作業

- [ ] 追加音声を実機で通し試聴し、違和感がある語は `scripts/generate-audio.mjs` の個別調整で再生成
- [ ] 分かりにくい絵は `codex_image_generation_guide.md` §4.3 の要領で個別再生成

## 保留(次の候補)

- 名詞カードの増量(スタッフから追加語リストを集める)。`data/pending/` に同じ形式で置けば `merge-pending.mjs` で取り込める
- 情景説明の出題の流れ(模範文を隠しておいて患者が説明した後に表示、など)は ST の使用感を聞いてから調整
- 短文理解は「文を聞いて情景絵を選ぶ」プリセット(選択モード × 情景 × 音声 × 2枚)で運用。独立した課題にするかは使用感を見て判断
